@model IEnumerable<ReportesWH.Models.siixsem_users_t>

@{
    Layout = null;
}

<!DOCTYPE html>

<html>
<head>
    <meta name="viewport" content="width=device-width" />
    <title>Index</title>
</head>
<body>
    <p>
        @Html.ActionLink("Create New", "Create")
    </p>
    <table class="table">
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.se_name)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.se_pass)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.se_id_rol)
            </th>
            <th></th>
        </tr>
    
    @foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.se_name)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.se_pass)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.se_id_rol)
            </td>
            <td>
                @Html.ActionLink("Edit", "Edit", new { id=item.se_id_user }) |
                @Html.ActionLink("Details", "Details", new { id=item.se_id_user }) |
                @Html.ActionLink("Delete", "Delete", new { id=item.se_id_user })
            </td>
        </tr>
    }
    
    </table>
</body>
</html>
