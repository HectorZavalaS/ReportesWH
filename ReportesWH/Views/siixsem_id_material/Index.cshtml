@model IEnumerable<ReportesWH.Models.siixsem_id_material>

@{
    Layout = null;
}

<!DOCTYPE html>

<html>
<head>
    <meta name="viewport" content="width=device-width" />
    <title>Index</title>
</head>
<body>
    <p>
        @Html.ActionLink("Create New", "Create")
    </p>
    <table class="table">
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.se_date)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.se_id_material)
            </th>
            <th></th>
        </tr>
    
    @foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.se_date)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.se_id_material)
            </td>
            <td>
                @Html.ActionLink("Edit", "Edit", new { id=item.se_id }) |
                @Html.ActionLink("Details", "Details", new { id=item.se_id }) |
                @Html.ActionLink("Delete", "Delete", new { id=item.se_id })
            </td>
        </tr>
    }
    
    </table>
</body>
</html>
